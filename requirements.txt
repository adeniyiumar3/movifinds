# MoviFinds — Python dependencies
# Install with: pip install -r requirements.txt

flask                        # web server
pandas                       # CSV loading and data processing
sentence-transformers        # local embedding model (~90MB download once)
chromadb                     # local vector database
scikit-learn                 # used internally by sentence-transformers
